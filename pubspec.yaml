name: get_location_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  build_runner: ^2.4.6
  flutter:
    sdk: flutter
  format: ^1.4.0
  hive: ^2.2.3
  hive_generator: ^2.0.1
  location: ^5.0.3
  path_provider: ^2.1.1
  permission_handler: ^11.0.1
  wakelock_plus: ^1.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
